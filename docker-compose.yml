services:
  app:
    build: .
    ports:
      - "8082:4000"
    env_file:
      - ./server/.env
    volumes:
      - wwebjs-auth:/app/.wwebjs_auth
    restart: unless-stopped

volumes:
  wwebjs-auth:
